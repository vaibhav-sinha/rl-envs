{
  "schema_version": 1,
  "screenshot": {
    "strategy": "auto"
  },
  "gates": {
    "require_change": true,
    "no_detached_nodes": true,
    "allowed_change_inside_ids": [
      "1655:195069"
    ]
  },
  "checks": [
    {
      "id": "must_contain_image_2",
      "type": "must_contain_image",
      "required": true,
      "scope": "new_frames"
    },
    {
      "id": "min_added_under_3",
      "type": "min_added_under",
      "required": true,
      "parent_id": "1655:195069",
      "min": 1
    }
  ],
  "visual": [
    {
      "id": "good_design_1",
      "type": "good_design"
    },
    {
      "id": "task_completeness_1",
      "type": "task_completeness"
    },
    {
      "id": "design_consistency_3",
      "type": "design_consistency",
      "reference_asset": "ref-design_consistency_3.png",
      "criteria": [
        "- Check if the sale section creates bundles similar in aesthetics to the reference. It does not need to look the same, but it should look part of the same app.",
        "- Check if the background is colored and has some pattern like this section. The color and pattern can be different.",
        "- Check if the horizontal scrollable container is shown similarly by having one full card and one partial card",
        "- Check if multiple product images are displayed in the card. While it does not need to be exactly the same as the reference, it should be in the same direction.",
        "- Check that based on the text, padding, border radius, shadow, etc the new section matches the reference."
      ]
    }
  ],
  "design_system": {
    "allow_novelty": {
      "text": {
        "color": false,
        "font_size": false,
        "font_family": false,
        "font_weight": false
      },
      "frames": {
        "fill": false,
        "background": true,
        "stroke_color": false,
        "stroke_width": false,
        "corner_radius": false,
        "effects": false,
        "item_spacing": false,
        "counter_axis_spacing": false,
        "padding": false,
        "layout_grid": false
      }
    }
  },
  "category_importance": {
    "commands": 0.15,
    "checks": 0.25,
    "design_system": 0.2,
    "visual": 0.5,
    "metadata": 0.1,
    "heuristics": 0.1
  }
}
