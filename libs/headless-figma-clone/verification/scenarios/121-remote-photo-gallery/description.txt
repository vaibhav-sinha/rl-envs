Three rectangles with IMAGE fills loaded from public HTTPS URLs via createImageAsync.

Expected: a horizontal strip of three distinct photos (cat, gull, checker PNG).

Clone verification requires HFC_ALLOW_NETWORK=1 on the MCP server.
